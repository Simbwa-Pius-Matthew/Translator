services:
  - type: web
    name: lugaflow
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /health
    envVars:
      - key: GOOGLE_TRANSLATE_API_KEY
        sync: false
